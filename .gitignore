node_modules
dist
dist-ssr
*.local
.vercel

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Capacitor Android: the native project IS committed (it holds custom native code —
# the DownloadsSaver plugin, largeHeap manifest, branded launcher icons). Build
# artifacts inside it are excluded by android/.gitignore.
